"""Reference values for tests and docs parity (never import from package code)."""

# OpenAI-compatible Chat Completions path on Groq. The ``groq`` SDK uses this
# route internally; see https://console.groq.com/docs/openai
GROQ_OPENAI_COMPAT_V1_BASE = "https://api.groq.com/openai/v1"
